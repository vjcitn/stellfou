% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basics.R
\name{efa_to_sf}
\alias{efa_to_sf}
\title{Convert EFA reconstructions to sf polygons in original coordinate space}
\usage{
efa_to_sf(efa_result, cell_ids = NULL, n_harmonics_use = NULL, n_points = 300)
}
\arguments{
\item{efa_result}{Output of compute_efa()}

\item{cell_ids}{Character vector of cell IDs to reconstruct (NULL = all valid)}

\item{n_harmonics_use}{Number of harmonics (NULL = all available)}

\item{n_points}{Number of points in the reconstructed contour}
}
\value{
An sf data.frame with columns cell_id and geometry (POLYGON)
}
\description{
The reconstruction uses the DC terms (a0/c0) stored in the raw EFA output,
so the returned polygons are in the same coordinate system as the original
cell segmentation geometries (e.g. Xenium stage coordinates in microns).
}
\examples{
make_ellipse <- function(cx, cy, a, b, n = 200L) {
  theta <- seq(0, 2 * pi, length.out = n + 1L)[-(n + 1L)]
  cbind(x = cx + a * cos(theta), y = cy + b * sin(theta))
}
boundaries <- list(
  cellA = make_ellipse(100, 200, 15, 8),
  cellB = make_ellipse(150, 220, 10, 10),
  cellC = make_ellipse(120, 260, 12,  6)
)
efa_result <- compute_efa(boundaries, n_harmonics = 6)
efa_to_sf(efa_result)
}
