% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basics.R
\name{plot_efa_interactive}
\alias{plot_efa_interactive}
\title{Interactive spatial plot of EFA reconstructions via ggplot2 and plotly}
\usage{
plot_efa_interactive(
  boundaries,
  efa_result,
  cell_ids = NULL,
  max_cells = 500L,
  n_harmonics_use = NULL,
  n_points = 300L,
  col_orig = "steelblue",
  col_recon = "firebrick"
)
}
\arguments{
\item{boundaries}{Named list of (n x 2) coordinate matrices (output of
extract_cell_boundaries())}

\item{efa_result}{Output of compute_efa()}

\item{cell_ids}{Cell IDs to include (NULL = all valid, up to max_cells)}

\item{max_cells}{Maximum number of cells when cell_ids is NULL}

\item{n_harmonics_use}{Number of harmonics (NULL = all available)}

\item{n_points}{Points in the reconstructed contour}

\item{col_orig}{Color for original boundaries}

\item{col_recon}{Color for EFA-reconstructed boundaries}
}
\value{
A plotly figure object
}
\description{
Renders original and EFA-reconstructed cell boundaries as an interactive
plotly figure. Pan and zoom into any subregion to inspect fit quality for
individual cells; hover to see the cell ID. Requires the ggplot2 and plotly
packages (listed under Suggests).
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE) &&
    requireNamespace("plotly",  quietly = TRUE)) {
  make_ellipse <- function(cx, cy, a, b, n = 200L) {
    theta <- seq(0, 2 * pi, length.out = n + 1L)[-(n + 1L)]
    cbind(x = cx + a * cos(theta), y = cy + b * sin(theta))
  }
  boundaries <- list(
    cellA = make_ellipse(100, 200, 15,  8),
    cellB = make_ellipse(150, 220, 10, 10),
    cellC = make_ellipse(120, 260, 12,  6)
  )
  efa_result <- compute_efa(boundaries, n_harmonics = 3)
  plot_efa_interactive(boundaries, efa_result)
}
}
