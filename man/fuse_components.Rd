% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion.R
\name{fuse_components}
\alias{fuse_components}
\title{Morphologically fuse disconnected components in a binary mask}
\usage{
fuse_components(mask, brush_size = 9)
}
\arguments{
\item{mask}{A binary matrix or 2D logical matrix.}

\item{brush_size}{Integer; size of the disk brush used for dilation (must be odd).}
}
\value{
A matrix of the same dimensions as \code{mask} with fused and labeled components.
}
\description{
Dilates a binary mask to bridge small gaps between components, maps the
dilated components back to the original mask to group connected pieces,
and takes their union to produce clean, fused geometries.
}
\examples{
# Create a small binary matrix with a gap
m <- matrix(0, nrow = 20, ncol = 20)
m[3:7, 5] <- 1
m[9:13, 5] <- 1  # Gapped vertical line
fused <- fuse_components(m, brush_size = 3)
print(fused[3:13, 5])
}
