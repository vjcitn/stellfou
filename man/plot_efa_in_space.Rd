% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basics.R
\name{plot_efa_in_space}
\alias{plot_efa_in_space}
\title{Plot EFA-reconstructed and original boundaries in original coordinate space}
\usage{
plot_efa_in_space(
  boundaries,
  efa_result,
  cell_ids = NULL,
  max_cells = 200L,
  n_harmonics_use = NULL,
  n_points = 300L,
  col_orig = "grey50",
  col_recon = "firebrick",
  lwd_recon = 1.5,
  ...
)
}
\arguments{
\item{boundaries}{Named list of (n x 2) coordinate matrices (output of
extract_cell_boundaries())}

\item{efa_result}{Output of compute_efa()}

\item{cell_ids}{Cell IDs to include (NULL = all valid, up to max_cells)}

\item{max_cells}{Maximum number of cells to plot when cell_ids is NULL}

\item{n_harmonics_use}{Number of harmonics (NULL = all available)}

\item{n_points}{Points in the reconstructed contour}

\item{col_orig}{Color for original boundaries}

\item{col_recon}{Color for reconstructed boundaries}

\item{lwd_recon}{Line width for reconstructed boundaries}

\item{...}{Additional arguments passed to plot()}
}
\value{
Invisibly returns an sf object of the reconstructed boundaries
}
\description{
Overlays original cell boundaries (grey) and their EFA reconstructions
(red) in a single base-R plot using tissue/stage coordinates so spatial
relationships between cells are preserved.
}
\examples{
make_ellipse <- function(cx, cy, a, b, n = 200L) {
  theta <- seq(0, 2 * pi, length.out = n + 1L)[-(n + 1L)]
  cbind(x = cx + a * cos(theta), y = cy + b * sin(theta))
}
boundaries <- list(
  cellA = make_ellipse(100, 200, 15, 8),
  cellB = make_ellipse(150, 220, 10, 10),
  cellC = make_ellipse(120, 260, 12,  6)
)
efa_result <- compute_efa(boundaries, n_harmonics = 6)
plot_efa_in_space(boundaries, efa_result)
}
